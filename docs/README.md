# DUNE run control (drunc) schema

This package contains the definitions of the protobuf messages that are used by `drunc`. The schema are defined for
 - controller to controller communication
 - controller application OpMon metrics
